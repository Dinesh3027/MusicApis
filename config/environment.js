module.exports = {
    CONFIG: {
        // user: process.env.NODE_ENV === 'development' && (os.userInfo() || {}).username || '',
        user: 'postgres',
        database: 'postgres',
        password: 'Zaq12wsx!',
        host: 'localhost',
        port: 5433,
    }
}